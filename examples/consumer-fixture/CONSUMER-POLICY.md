# Consumer policy fixture

This file represents repository-owned policy. Pi-next does not provide or
assume this content; a real consumer should replace it with its own review,
test, and deployment rules.

The fixture deliberately contains no Pi-next runtime source, extension copy,
submodule, or authority credentials.
