# Issue #87 plan

- [x] Add self-contained, lockfile-aware dependency preparation before workers/checks.
- [x] Add verify-only grading for the canonical existing worktree without an implementation turn.
- [x] Report bounded candidate state (dirty/committed/uncommitted/changed files).
- [x] Correct nested Pi SDK token/cost/tool telemetry and flag suspicious usage.
- [x] Add regression coverage for setup, preservation, candidate reporting, telemetry, and verify-only behavior.
- [x] Run `npm run typecheck` and `npm test`.
