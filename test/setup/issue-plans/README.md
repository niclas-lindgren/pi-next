# Historical issue-plan fixtures

These completed issue plans are test data for legacy workflow/recovery scenarios.
They intentionally live below `test/setup/` and are not production workflow state;
production plans belong in the configured issue workspace (normally
`.pi-next/PLAN.md`).
