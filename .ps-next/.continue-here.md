# Continue Here

## Context
- source: auto-worker
- stage: generic
- task: Delete ForegroundSupervisor and loop-controller.ts legacy state machine (issue #165)
- plan_goal: 
- unchecked: 2
- reason: ForegroundSupervisor.recoverOnStart(ctx) is called from commands-recovery.ts on every plain /pi-next auto invocation (not just explicit resume) to detect and auto-recover abandoned runs after a crash — it internally calls .launch() itself to drive the recovered run via the legacy step-based loop-controller.ts machinery, including preserving in-progress dirty files. Task 5 already made the explicit resume/stop paths route through the unified scheduler or reject legacy state, but this startup-ti

## Written
2026-08-25T20:03:10Z

## Next steps
- Inspect $PS_DIR/PLAN.md
- Run /PS-next when ready to resume; the marker is not auto-deleted
